# Memory Workstation 第三方依赖
# 安装: pip install -r requirements.txt
# 打包: build.bat

# --- 核心框架 ---
pystray>=0.19.0          # 系统托盘
watchdog>=4.0.0          # 文件监控
fastapi>=0.110.0         # HTTP API
uvicorn[standard]>=0.29.0# HTTP 服务器
requests>=2.31.0         # HTTP 客户端

# --- 向量存储 ---
lancedb>=0.9.0           # LanceDB 向量数据库
pyarrow>=16.0.0          # LanceDB 依赖

# --- 本地 LLM ---
llama-cpp-python>=0.2.80 # 本地模型推理 (GGUF)

# --- 可选: 云端 LLM ---
# 使用云端时不需要 llama-cpp-python

# --- 自然语言处理 ---
jieba>=0.42.1            # 中文分词
tiktoken>=0.7.0          # token 精确计数 (cl100k_base 编码)
transformers>=4.40.0     # 可选: HF tokenizer
onnxruntime>=1.17.0      # 可选: ONNX 推理

# --- MCP 协议 ---
mcp>=1.0.0               # MCP 服务器

# --- 工具 ---
Pillow>=10.0.0           # 图片处理 (pystray 依赖)
tomli>=2.0.0             # TOML 解析 (Python 3.11+ 已内置)
ttkbootstrap>=1.20.3     # 现代化 UI 主题
